package com.ubcsc.checkout.ui.theme

import androidx.compose.ui.graphics.Color

// ------------------------------------------------------------------
// Copper & Slate palette
// ------------------------------------------------------------------

// Backgrounds
val DeepOcean        = Color(0xFF141618)   // near-black slate — main background
val OceanSurface     = Color(0xFF1E2228)   // elevated surface
val CardBlue         = Color(0xFF1E2228)   // card background

// Brand copper
val TealLight        = Color(0xFFE8A96A)   // warm copper highlight
val TealMid          = Color(0xFFB87333)   // primary copper action
val TealDark         = Color(0xFF8B5E2A)   // pressed / darker copper

// Accent
val NavyAccent       = Color(0xFF1A1E22)

// Status
val AvailableGreen   = Color(0xFF43A047)
val UnavailableRed   = Color(0xFFEF4444)
val ActiveAmber      = Color(0xFF34D399)   // emerald — "currently out" indicator

// Text
val TextPrimary      = Color(0xFFFFFFFF)
val TextSecondary    = Color(0xFFF5CFA0)   // warm cream
val TextMuted        = Color(0xFF6B7280)   // cool grey

// Utility
val White            = Color(0xFFFFFFFF)
val DividerColor     = Color(0xFF2C333B)
