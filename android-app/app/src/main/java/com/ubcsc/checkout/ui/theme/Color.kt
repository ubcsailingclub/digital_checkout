package com.ubcsc.checkout.ui.theme

import androidx.compose.ui.graphics.Color

// ------------------------------------------------------------------
// Dark ocean palette — inspired by the UBC Sailing Logbook color scheme
// ------------------------------------------------------------------

// Backgrounds
val DeepOcean        = Color(0xFF0D1B2A)   // near-black navy — main background
val OceanSurface     = Color(0xFF1B2A3B)   // elevated surface (app bar, bottom sheets)
val CardBlue         = Color(0xFF162032)   // card background

// Brand teal (from logbook navigation gradient)
val TealLight        = Color(0xFF4DD0E1)   // highlight, icons on dark
val TealMid          = Color(0xFF00ACC1)   // primary action
val TealDark         = Color(0xFF00838F)   // pressed / variant

// Accent — matches logbook primary color (#1e1a29 navbar)
val NavyAccent       = Color(0xFF1E1A29)

// Status
val AvailableGreen   = Color(0xFF43A047)
val UnavailableRed   = Color(0xFFE53935)
val ActiveAmber      = Color(0xFFFFB300)   // currently checked-out indicator

// Text
val TextPrimary      = Color(0xFFFFFFFF)
val TextSecondary    = Color(0xFF90CAF9)   // soft blue-white
val TextMuted        = Color(0xFF546E7A)   // dimmed

// Utility
val White            = Color(0xFFFFFFFF)
val DividerColor     = Color(0xFF263749)
